<div class="row mt-4">
<div class="col-3">
    <img class="w-100" id="avatar" src="http://arturober.com/svtickets-services/public/img/profile/{{avatar}}" alt="">
</div>
<div class="col-9">
    <h4>{{name}}</h4>
    <h4>
    <small class="text-muted">{{email}}</small>
    </h4>
    <p>
    {{#if me}}
    <a href="edit-profile.html" class="btn btn-primary">Edit profile</a>
    {{/if}}
    </p>
</div>
</div> 
